# --- Phase 1-2 dependencies (project setup + FIRMS ingestion) ---
python-dotenv>=1.0.0
PyYAML>=6.0
requests>=2.31.0
pandas>=2.0.0

# --- India boundary filtering (geospatial: point-in-polygon, CRS handling) ---
geopandas>=0.14.0

# --- Testing ---
pytest>=7.4.0
httpx>=0.24.0

# --- ML / AI / CNN ---
scikit-learn>=1.3.0
joblib>=1.3.0
torch>=2.0.0
torchvision>=0.15.0

# --- GIS / Visualization ---
folium>=0.14.0

# --- FastAPI Service Layer ---
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0